---
# file: playbook.yml
#   Use this file to setup components required for galaxy

# To install the software, use this role via the following command:
#
#   % ansible-playbook playbook.yml
#
- hosts: localhost
  connection: local
  sudo: yes
  roles:
    - jcu
  tags: jcu
